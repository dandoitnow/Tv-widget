<?xml version="1.0" encoding="utf-8"?>
<!--
  5 columns x 2 rows. The design canvas is 360 x 152dp; minWidth/minHeight are the smaller floor a
  launcher may hand us, and the widget is resizable in both directions from there.

  targetCellWidth/Height and previewLayout are API 31+ only. That is deliberate rather than an
  oversight: each one degrades cleanly on older releases (to minWidth/minHeight and previewImage
  respectively), so the newer attributes are declared unconditionally instead of duplicating this
  file into an xml-v31 variant to say the same thing twice.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="UnusedAttribute"
    android:minWidth="320dp"
    android:minHeight="110dp"
    android:targetCellWidth="5"
    android:targetCellHeight="2"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:previewLayout="@layout/widget_preview_layout"
    android:previewImage="@drawable/widget_preview"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
